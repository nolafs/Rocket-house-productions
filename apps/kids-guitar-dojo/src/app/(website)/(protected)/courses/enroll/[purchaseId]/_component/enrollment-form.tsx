import React from 'react';

interface EnrollmentFormProps {
  action: string;
}

export const EnrollmentForm = ({ action }: EnrollmentFormProps) => {
  return <div> {/* Your code here */} </div>;
};

export default EnrollmentForm;
